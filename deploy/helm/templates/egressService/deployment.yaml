{{ if .Values.egressServiceInstall }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.egressService.name }}
  labels:
    release: {{ .Release.Name }}
    {{- with .Values.egressService.deploymentLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  replicas: {{ .Values.egressService.replicaCount }}
  selector:
    matchLabels:
      {{- toYaml .Values.egressService.deploymentSelectorMatchLabels | nindent 6 }}
  template:
    metadata:
      labels:
        release: {{ .Release.Name }}
        {{- with .Values.egressService.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      containers:
        - name: {{ .Values.egressService.name }}
          image: {{ .Values.egressService.image }}:{{ .Chart.AppVersion }}
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.egressService.port }}
          {{- if .Values.egressService.resources }}
          resources:
            {{- toYaml .Values.egressService.resources | nindent 12 }}
          {{- end }}
          readinessProbe:
            tcpSocket:
              port: {{ .Values.egressService.port }}
            initialDelaySeconds: 15
            periodSeconds: 10
{{ end }}