{{ if .Values.egressServiceInstall }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.egress-service.name }}
  labels:
    release: {{ .Release.Name }}
    {{- with .Values.egress-service.deploymentLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  replicas: {{ .Values.egress-service.replicaCount }}
  selector:
    matchLabels:
      {{- toYaml .Values.egress-service.deploymentSelectorMatchLabels | nindent 6 }}
  template:
    metadata:
      labels:
        release: {{ .Release.Name }}
        {{- with .Values.egress-service.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      containers:
        - name: {{ .Values.egress-service.name }}
          image: {{ .Values.egress-service.image }}:{{ .Chart.AppVersion }}
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.egress-service.port }}
          {{- if .Values.egress-service.resources }}
          resources:
            {{- toYaml .Values.egress-service.resources | nindent 12 }}
          {{- end }}
          readinessProbe:
            tcpSocket:
              port: {{ .Values.egress-service.port }}
            initialDelaySeconds: 15
            periodSeconds: 10
{{ end }}