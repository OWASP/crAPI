{{ if .Values.egressServiceInstall }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.egressService.service.name }}
  labels:
    release: {{ .Release.Name }}
    {{- with .Values.egressService.service.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  ports:
  - port: {{ .Values.egressService.port }}
    name: https
  selector:
    {{- toYaml .Values.egressService.serviceSelectorLabels | nindent 4 }}
{{ end }}